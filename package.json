{
  "name": "multi-vendor-platform",
  "version": "1.0.0",
  "description": "Multi-Vendor MERN Platform with Dynamic Dashboards and QR Membership Cards",
  "main": "server.js",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "postinstall": "npm install --prefix frontend",
    "simulate": "node backend/utils/simulate-order.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop connect-backend",
    "pm2:restart": "pm2 restart connect-backend",
    "pm2:logs": "pm2 logs connect-backend",
    "pm2:status": "pm2 status"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "localtunnel": "^2.0.2"
  }
}
